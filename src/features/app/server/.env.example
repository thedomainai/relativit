# ============================================
# Server Configuration
# ============================================
PORT=3001
NODE_ENV=development

# ============================================
# Database Configuration
# ============================================
# Supabase connection string (Session Pooler)
# Get from: Supabase Dashboard > Settings > Database > Connection Pooling > Session mode
DATABASE_URL="postgresql://postgres:[YOUR-PASSWORD]@[YOUR-HOST]:5432/postgres?sslmode=require"

# ============================================
# Security (IMPORTANT: Generate strong values for production!)
# ============================================
# Generate JWT_SECRET: openssl rand -base64 64
JWT_SECRET=change-me-to-a-long-random-secret-minimum-64-characters

# Generate ENCRYPTION_KEY: openssl rand -base64 32
ENCRYPTION_KEY=change-me-to-a-32-byte-encryption-key

# ============================================
# Email Configuration (Resend)
# ============================================
# Development: Leave empty to use onboarding@resend.dev automatically
# Production: Use verified domain (e.g., noreply@relativit.app)
EMAIL_FROM=
RESEND_API_KEY=your-resend-api-key-here

# ============================================
# CORS & App URL
# ============================================
# Development
CORS_ORIGIN=http://localhost:3000
APP_URL=http://localhost:3000

# Production (uncomment and set for production)
# CORS_ORIGIN=https://relativit.app
# APP_URL=https://relativit.app

# ============================================
# Relativit Trial Mode
# ============================================
# API keys used when users enable trial mode (without their own API key)
RELATIVIT_API_KEY=your-relativit-api-key-here
RELATIVIT_API_PROVIDER=anthropic  # Options: anthropic, openai, gemini

# ============================================
# Feature Flags
# ============================================
# Demo mode (for local testing - uses fixed verification code)
# Set to false in production
DEMO_MODE=false
